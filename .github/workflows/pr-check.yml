name: pr-check

on:
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write
  checks: write
  security-events: write

concurrency:
  group: pr-check-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    uses: guilhermelinosp/templates/.github/workflows/shellcheck.yml@9a3e1c923824fbd71b5dad45976c6e66ba2a1a01 # main

  merge-check:
    uses: guilhermelinosp/templates/.github/workflows/merge-check.yml@9a3e1c923824fbd71b5dad45976c6e66ba2a1a01 # main

  gitleaks:
    uses: guilhermelinosp/templates/.github/workflows/gitleaks.yml@9a3e1c923824fbd71b5dad45976c6e66ba2a1a01 # main

  labeler:
    uses: guilhermelinosp/templates/.github/workflows/labeler.yml@9a3e1c923824fbd71b5dad45976c6e66ba2a1a01 # main


  govulncheck:
    uses: guilhermelinosp/templates/.github/workflows/govulncheck.yml@9a3e1c923824fbd71b5dad45976c6e66ba2a1a01 # main

# CodeQL runs via the per-repo inline workflow (.github/workflows/codeql.yml):
# the reusable variant still triggers GitHub-side startup_failure across
# callers regardless of content/pinning; revisit after a clean-room repro.
